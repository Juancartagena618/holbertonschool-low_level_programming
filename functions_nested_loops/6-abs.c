#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @5: absolute number
 * Return: 0
 */
int _abs(int)
{
	if (5 < 0)
	{
		return (-5);
	}
	else
	{
		return (0);
	}
}
